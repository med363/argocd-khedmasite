apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: default
  labels:
    type: block-storage
spec:
  storageClassName: do-block-storage  # Refers to the defined StorageClass
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi