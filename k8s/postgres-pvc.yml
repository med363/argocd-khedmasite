apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: default
  labels:
    type: block-storage
spec:
  storageClassName: do-block-storage
  accessModes:
    - ReadWriteOnce # DigitalOcean block storage supports RWO
  resources:
    requests:
      storage: 2Gi